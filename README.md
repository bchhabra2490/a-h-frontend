## How to setup locally

1. Update env variables if needed. It looks for `NEXT_PUBLIC_WS_URL=` only. By default, listens to localhost:3001 but if needed, can be updated
2. `npm install`
3. `npm run dev`